# AGENTS.md

## 适用范围

本文件适用于整个仓库。Codex 在任何任务开始前都必须先读取本文件，并按下述顺序加载项目上下文。

## 必读顺序与权威关系

1. `AGENTS.md`
2. `CODEX_CONTEXT.md`
3. `docs/governance/PINN_PCM_Norms_2026-08-14.md`
4. `rules.md`
5. `active_phase.md`
6. `PROJECT_STATE.md`
7. `NEXT_ACTIONS.md`

上位系统指令与用户当前明确指令优先。项目规范中的“硬约束”只有用户明确修改时才能改变；若新任务与硬约束明显冲突，停止冲突部分并向用户提出最小决策问题。项目规范中的“默认偏好”可被更具体、更新的任务规则覆盖。状态与计划文件只能约束当前阶段，不能覆盖项目规范。

## 项目目标

- 研究主题始终是物理信息神经网络（PINN）与氧化物相变材料、相变器件、忆阻器或神经形态计算的交叉。
- 最终交付目标是从 idea 筛选推进到证据闭合、可复现、达到中科院二区 SCI 定位的论文初稿；这不是期刊接收承诺。
- 研究采用纯软件闭环。合成数值结果、文献事实、实现状态和实验验证必须严格区分。
- 学术研究优先，工程管理服务于研究；提交、推送、合并和 CI 状态不得表述为科学成果。

## 每次任务的工作规则

- 先确认 `active_phase.md` 的授权边界，再执行任务；不得从计划、历史记录或仓库中已有的实现推断额外授权。
- 研究任务开始前，应明确其论文去向、预期证据、强基线、关键消融、formal OOD 或实体级拆分，以及停止条件。
- 重大科学任务遵循 PLAN → 用户批准 → EXECUTE；长时间求解、训练、外部写入或预算扩张必须获得明确授权。
- 采用最小、可逆、与当前目标直接相关的修改，不进行无关重构、无界搜索、救援式重复运行或装饰性治理。
- 只在输入、实现或合同发生相关变化后重跑验证；达到预声明的通过、失败或阻塞条件后收口。
- 不以字节数、行数、文件数、文档字数、重复哈希检查或同类审计次数代替实质判断。

## 科学与证据底线

- 只有损失中包含明确、可记录的 PDE 或本构残差时才称为 PINN。
- 正面方法路线必须保持 PINN 的核心作用，并证明算法或网络相对强基线的可测增量。
- 论文主体不得退化为抽象方域、一维模型或单节点模型；至少闭合电—热—相态因果链。
- 数据按完整器件、几何、协议、轨迹、时间窗或其他真实实体拆分，防止测试信息泄漏。
- 已验证事实、证据支持的解释、假设和未知必须分开；负面结果按其证据边界保留，不夸大也不掩饰。
- 文献、参数、代码和外来资产必须保留来源与许可；不得编造、抄袭、隐藏来源或虚报优先权。

## 仓库与交付

- 科学代码、配置、数据、运行产物和论文主张之间必须可追溯，但治理文件不得冒充研究证据。
- 重要研究过程和阶段成果可在学术产物收口后单独提交到云端；云端操作与科学结论分开汇报。
- 不主动提交、推送、开 PR 或合并，除非当前任务或阶段明确要求。
- 使用 Windows 可达路径、Python 3.11、项目虚拟环境和 CPU 冒烟测试作为默认基础环境；GPU 仅用于通过前置门槛后的高价值工作。

## 表达方式

- 结论优先、简洁直接，只报告有决策价值的进展、证据、异常和下一步。
- 对任何科学结论标明状态：`VERIFIED`、`SUPPORTED_INTERPRETATION`、`HYPOTHESIS` 或 `UNKNOWN`。
- 若项目文件之间出现不一致，指出具体冲突和受影响的动作，不静默调和。
