# AGENTS.md

## 适用范围

本文件适用于整个仓库。Codex 在任何任务开始前都必须先读取本文件，并按下述顺序加载项目上下文。

## 必读顺序与权威关系

1. `AGENTS.md`
2. `CODEX_CONTEXT.md`
3. `docs/README.md`
4. `docs/governance/PINN_PCM_Norms_2026-08-14.md`
5. `rules.md`
6. `active_phase.md`
7. `PROJECT_STATE.md`
8. `docs/plans/NEXT_ACTIONS.md`

`docs/README.md` 是文档地图，不单独授权科研动作。上位系统指令与用户当前明确指令优先。项目规范中的“硬约束”只有用户明确修改时才能改变；若新任务与硬约束明显冲突，停止冲突部分并向用户提出最小决策问题。项目规范中的“默认偏好”可被更具体、更新的任务规则覆盖。状态与计划文件只能约束当前阶段，不能覆盖项目规范。

## 项目目标

- 研究主题始终是物理信息神经网络（PINN）与氧化物相变材料、相变器件、忆阻器或神经形态计算的交叉。
- 最终交付目标是从 idea 筛选推进到证据闭合、可复现、达到中科院二区 SCI 定位的论文初稿；这不是期刊接收承诺。
- 研究采用纯软件闭环。合成数值结果、文献事实、实现状态和实验验证必须严格区分。
- 学术研究优先，工程管理服务于研究；提交、推送、合并和 CI 状态不得表述为科学成果。

## 每次任务的工作规则

- 先确认 `active_phase.md` 的授权边界，再执行任务；不得从计划、历史记录或仓库中已有的实现推断额外授权。
- 研究任务开始前，应明确其论文去向、预期证据、强基线、关键消融、formal OOD 或实体级拆分，以及停止条件。
- 重大科学任务遵循 PLAN → 用户批准 → EXECUTE；长时间求解、训练、外部写入或预算扩张必须获得明确授权。
- 采用最小、可逆、与当前目标直接相关的修改，不进行无关重构、无界搜索、救援式重复运行或装饰性治理。
- 只在输入、实现或合同发生相关变化后重跑验证；达到预声明的通过、失败或阻塞条件后收口。
- 不以字节数、行数、文件数、文档字数、重复哈希检查或同类审计次数代替实质判断。

## HERO 反过度防御（项目适配 A′）

本节源自 `HERO-Anti-OverDefense` 的项目适配，只约束附加流程和拟议修法，不限制发现、报告或解决本项目真实可达的问题，也不构成研究授权。项目硬约束、用户明确要求、本机安全边界和 `active_phase.md` 始终优先。

- 新增检查、审计、守卫或版本化产物前，先说明仍然存活的不确定性、它能检出的具体失败，以及失败后哪项行动会改变；无法给出实质答案时不执行。无法预知具体消费者但能界定受影响范围时，可用该有界失败类别作为理由。
- 真实问题必须报告，但修复应与影响相称：局部工程失败优先在当前实现和当前工作根内最小修复；只有科学语义改变，或已被正式下游消费的结果必须保持可复现时，才建立新的持久版本。
- 不因一个局部异议或失败推翻未被挑战的研究方向；先修复被指出的部分并保留其余内容，除非证据已经触发预声明停止条件。
- 正确时明确说正确，不为证明流程完整而制造 finding、未知项或额外工作。与科学结论直接相关的来源核验、防泄漏、数值有效性、必要复验，以及真实安全或破坏性操作边界不属于过度防御。

## 科学与证据底线

- 只有损失中包含明确、可记录的 PDE 或本构残差时才称为 PINN。
- 正面方法路线必须保持 PINN 的核心作用，并证明算法或网络相对强基线的可测增量。
- 论文主体不得退化为抽象方域、一维模型或单节点模型；至少闭合电—热—相态因果链。
- 数据按完整器件、几何、协议、轨迹、时间窗或其他真实实体拆分，防止测试信息泄漏。
- 已验证事实、证据支持的解释、假设和未知必须分开；负面结果按其证据边界保留，不夸大也不掩饰。
- 文献、参数、代码和外来资产必须保留来源与许可；不得编造、抄袭、隐藏来源或虚报优先权。

## 仓库与交付

- 科学代码、配置、数据、运行产物和论文主张之间必须可追溯，但治理文件不得冒充研究证据。
- 重要研究过程和阶段成果可在学术产物收口后单独提交到云端；云端操作与科学结论分开汇报。
- 不主动提交、推送、开 PR 或合并，除非当前任务或阶段明确要求。
- 使用 Windows 可达路径、Python 3.11、项目虚拟环境和 CPU 冒烟测试作为默认基础环境；GPU 仅用于通过前置门槛后的高价值工作。
- 修改 `README.md`、`active_phase.md`、`PROJECT_STATE.md`、唯一 live plan、ADR 或实验索引后，必须运行 `.\.venv\Scripts\python.exe -m pinn_pcm_sci.document_consistency --root .`；门禁失败时不得交付、提交或继续科研执行。

## 外部 Skill

- 外部 Skill 始终从属于本项目权威链；安装、存在或显式调用均不构成研究授权，也不能覆盖 `active_phase.md`。
- 使用 `.agents/skills/` 下的外部 Skill 或集成到 `AGENTS.md` 的外部规则前，先读取其项目绑定说明和 `docs/governance/EXTERNAL_SKILLS.md`；来源、许可、固定版本、适配和运行期外联必须可追溯。
- ResearchStudio Skill 仅允许显式调用。模型回忆、自动评分和工作流 verdict 只能作为待核验线索，不能替代原始来源、科学证据、Phase 0 筛选合同或用户批准。
- 不运行会关闭审批、放宽沙箱、继承全部环境、递归覆盖目录、写入明文凭据或升级全局依赖的上游安装器。

## 表达方式

- 结论优先、简洁直接，只报告有决策价值的进展、证据、异常和下一步。
- 对任何科学结论标明状态：`VERIFIED`、`SUPPORTED_INTERPRETATION`、`HYPOTHESIS` 或 `UNKNOWN`。
- 若项目文件之间出现不一致，指出具体冲突和受影响的动作，不静默调和。
