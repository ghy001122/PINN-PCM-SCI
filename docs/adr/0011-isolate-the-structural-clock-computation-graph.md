# 隔离结构时钟计算图

首篇方法将结构时钟、结构序参量 \(\eta\) 表示和其余物理时间场表示实现为互不共享可训练参数的模块；\(\eta\) 不读取原始时间、驱动、动态场、\(K_\eta\)、\(\tau_t\) 或可重构时间的 latent，其余场也不读取时钟或 \(\eta\) latent，场间耦合只通过冻结的 PDE、本构和电路残差发生。该选择牺牲共享表征的潜在效率，换取无前向时间旁路、可审计的链式关系和可归因的 field-selective clock 机制；所有 learned-clock 负控沿用相同分块，raw-time 另做活动容量匹配。
