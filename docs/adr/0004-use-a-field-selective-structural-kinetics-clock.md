# 只对结构序参量使用动力学时钟

首篇方法只将结构序参量 \(\eta\) 表示在由完整 \(K_\eta\) 约束的局部单调时钟上，其他物理场继续使用物理时间；\(\eta\) 不直接读取原始时间、驱动协议或动态场输出，耦合仅通过冻结的 Q-POP 残差发生，并在所有相关残差中使用完整回拉后的 \(\eta\) 导数。该选择以放弃全场统一或多时钟架构，换取不扭曲电热时间尺度、可检验的斜率归一化条件和清晰的机制归因。
