# 下一研究计划：以本轮匹配证据决定最小后续

- `phase_id`: `PHK_V23_LF11_REMAINING_PDE_COUNTERFACTUAL_COMPLETE`
- `lifecycle_state`: `COMPLETE`
- `blocker_id`: `NONE`
- `claim_status`: `VALID_MEASURED_PDE_INFLUENCE_NO_MATCHED_PREDICTIVE_INCREMENT`
- `next_research_execution_authorized`: `false`

**PROPOSED_NOT_AUTHORIZED。** 本轮已完整结束；没有待补的科学运行或需要重开的实例。保留电学消元、同层强基线和 P1 的非劣结果；没有符合本轮条件的 P_F 方法分支。以下为下一次研究的具体提案，不是当前执行权限。

## 首要问题与选择

最优先问题是：**当前热主导的剩余目标，是否在可信的数值尺度上约束了与器件预测相关的误差？** P_kappa 对 P1 的审计 J 改善只有2.19%，功率却恶化4.90%，相态审计还略高1.51%。这不足以支持立即换网络，也不能证明细网格参考与新热单元目标不相容。

首选一次冻结函数的数值适配实验，直接量化这条仍存活的不确定性；不再进行同目标优化、梯度全扫描或权重搜索。它检查 V28 新电学/热单元接口，不能用更早 C0 的旧强式结论直接代替。以下阈值用于新提案的数值解释，不改变任何旧 A/B、事件门或已发表角色。

## 一个最小、零更新实验

1. **输入固定。** 只用保存的 D_E、D_C、P1、P_kappa 四个函数、原 audit-pool 的32个时刻和原物理位置。保留80×40热控制体、全部已知物理和原相态 AD 方程；不增加观测、teacher 标签或训练。旧 nominal 参考只保持既有评价身份，不能当连续体真值。
2. **两个明确数值因素。** 在同一冻结函数上比较：①原热单元均值/面中点与二点高斯面求积、2×2高斯单元均值；②80×40与160×80电学求解/焦耳沉积，将细网格 q 按体积保守聚合到相同80×40热控制体。只作2×2固定函数评价，分别分离求积和电学分辨率影响；同一电学分解/沉积在两种热求积中缓存复用。phase 残差位置、尺度及权重均不变，不静默改训练算子。
3. **预算与边界。** 零 optimizer、零参数梯度/伴随、零完整热/phase 轨迹求解；四函数×32时刻×两种电网，最多256次电学正解，零驱动解析跳过，禁止追加时刻或第二轮扫描。坐标 AD 仅用于已声明的热/phase 函数导数，不作参数诊断。所有新数值因素与输出在执行前冻结；没有新参考选模。
4. **主输出和反证。** 一张“数值规则变化幅度 vs P_kappa−P1 方法差值”图，列原尺度热/phase/J、四时间窗与原 ROI/外部的同权重分项。若规则变化使匹配 J 差值换号，或规则变化幅度达到原匹配差值的量级，则现有残差优势只能保留为原算子特定结果；不得继续宣称物理精度改善。若排序与幅度在两因素下稳定，也只支持这两个数值设置间的稳健性，不证明连续体收敛，更不能把2.19%残差收益升级为器件收益。
5. **只允许一次后续决策。** 若定位到单个明确接口问题，提出其最小修正及“原/修正热接口×有/无剩余 PDE”的同父对照，另冻结预算并申请用户授权，保留原配置和结果。若没有这种证据，关闭数值适配疑点，停止用小改动救援当前强监督重建任务；下一份 PLAN 应先说明一个真实缺失信息或参数识别需求及同信息传统求解/直接基线，不自动减少观测、隐藏周期或复用旧权重制造盲测。

## 论文推进与保留条件

当前 [paper_v29](../../paper/paper_v29/README.md)已整合实际机制与预测证据，可继续做学术讨论和原始来源核对。优先写清“电学修复、学习收益、剩余 PDE 增量”三个可区分主张；第一周期支持越门、第二周期时刻改善与功率代价均保留，不以门失败抹去局部效果，也不把其升级成方法总体获胜。

P_F 当前不提为救援实验；两套从头初始化、干净的新观测规则、完整新协议及无事件对照仍是首次可信独立方法信号后的确认要求。未冻结新预算、未选择新掩码、未启动任何确认。新的氧化物材料关联也必须单独核对本构和同一来源参数；当前对象仍是 synthetic dimensionless PCM-inspired wall-cell。

本轮完整事实见[终局](../experiment/2026-09-13-phk-v23-lf11-remaining-pde-terminal-closeout.md)，完成的执行计划另存[历史记录](../../archive/2026-09-13-lf11-remaining-pde-executed-plan.md)。用户随后明确授权本轮成果提交、推送及[V29 独立复评交付](../notes/2026-09-13-lf11-v29-results-cloud-review-handoff.md)；对方可反驳本提案，评估与 Git 发布不产生任何新科学执行授权。
