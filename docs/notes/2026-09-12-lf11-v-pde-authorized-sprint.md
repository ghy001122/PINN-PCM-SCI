任务：执行 PINN×PCM 下一轮有界研究冲刺，尽快获得可信的内部 PDE 增量及可入稿证据。
工作根：E:\Python demo\PINN-PCM-SCI
继承版本：8a0f1a0952db57de2b5d0907e26981dcd3264076。

本指令授权以下实现、训练、分析和本地论文更新。先简要确定实施顺序，随后连续执行；范围内不反复请示。改变物理、增加观测、读取stress、突破预算或新增外部权限才询问。不自动commit/push/PR。

一、直接承接，避免重复工作
- 读取AGENTS、当前状态、paper_v25及必要源码，快速确认有无更新；已有有效结果直接复用，不重跑S0/S1，不做全仓审计或重复SHA检查。
- 当前温度拟合已达标，phase未变；拟合阻塞是V RMS/0.72=0.0082151003，高于0.005。新D_B/P_U和电学归一化尚未运行，不能称失败。
- 保持原二维物理、双脉冲、本构、IC/BC、sparse输入和正式指标。禁止dense权重、完整参考场、teacher电流/功率、stress进入训练。
- 所有旧观测已被使用，只称训练/开发数据；不能重新划切片冒称未见验证。
- 重要选择先分析“瓶颈—唯一改动—反事实”。复用FP64、Adam→固定目标L-BFGS、已有T适配器等trick。
- 不报告GPU价格、费用或时长。GPU使用阶段结束后立即回收产物、终止任务并及时关闭实例，确认后才本地读取参考评价，不空挂等分析。

二、第一阶段：仅延续V拟合，最多200次完整评估
输入：
paper/paper_v25/evidence/fixed-fit-endpoint.pt
paper/paper_v25/evidence/optimizer-states/refinement_lbfgs_V.pt
paper/paper_v24/evidence/input/sparse.npz
本地对应原文件存在时优先复用。

通过fit_model加载最终模型，保留最终T适配器、T/phase函数；只开放V头。
仅恢复与最终V权重相容的V L-BFGS状态，不整包加载旧模型覆盖最终T。
一次核对V参数逐项一致、顺序/shape和优化器字段；相容则延续历史，不相容则明确记录fresh restart，仍使用同一200次总预算，不增加restart比较臂。

目标保持：
L_V=(1/3)Σp_i[(Vθ−Vobs)/0.72]²。
不改结构、目标、观测测度或波形，不增加BC/PDE、不加Adam。
使用现有固定完整目标、真实加权分块、strong-Wolfe及接受状态回滚；closure不重采样、不动态调权、不裁剪梯度。
所有初始、重复、线搜索试探的完整目标/梯度评估均计入200次。

停止规则在执行前冻结：
首个已接受状态满足L_V≤0.005²/3时立即停止并保存repaired_parent；这是训练准入，不是按reference选模。
否则在预算耗尽、正常收敛、无接受进展或数值失败时停止，保留最后接受状态。不能把试探点当终点，也不能再追加一轮200。
T/phase及适配器保持不变。仍未过门则不运行后续PDE分支、不移动0.5%门，完成本轮定量收口并提出一个最小下一决策。

三、V-only前后：一次底流分解，不增加准入门
使用固定坐标和模型自身输出，不增加标签、不求PDE。保存：
1. heater预测边界迹及底部AD法向通量；
2. 正式FV底流的精确分解：
   v_i为首层cell预测电势，b_i为同横坐标的预测边界迹，
   c_i=2σ_i A_i/h_z，
   I_b=Σc_i b_i+Σc_i(v_i−b_i)；
3. 底部耗散三项：
   P_b=Σc_i b_i²+2Σc_i b_i(v_i−b_i)+Σc_i(v_i−b_i)²；
   同时报内部、顶部、底部耗散。

保存带符号时间序列及交叉项，不能把RMS直接相加或当独立贡献率。
b_i不是人为设为0；AD通量不是真值；这些分量不能替换正式FV电流或扣除后再评分。
诊断回答：V-only的读出改善来自边界迹、近边界电压增量还是抵消？在相同T/phase、相同σ下，才能归因于V变化。
0.5%体积拟合不保证通量正确；通量未过关不阻塞已达标父状态进入PDE比较。

同时冻结一个低成本强基线B_logit_waveform_contact：
保留原逐周期V/U正电压PCHIP、端点保持、空间线性插值及解析U规则；
在底边显式补入已知heater端点x=±a_h、V=0，保证整个heater段的分片线性Dirichlet值正确。新增网格位置的其他值来自原插值，不增加标签；T/phase不变。
核对原可见观测保持；不声称绝缘通量也被精确满足。旧B_logit_waveform和历史表保留，新增基线单列。

四、V达标后立即执行同父D_B/P_U
两臂从同一个repaired_parent启动；三头及T适配器恢复联合训练，使用fresh Adam，随后各自fresh L-BFGS，不继承V-only曲率历史。

D_B：观测＋相同BC/IC，无内部PDE。
P_U：只增加原始uniform内部PDE。
父状态→D_B同时包含继续优化和phase解冻，不能单独称为BC效应；内部PDE增量只由P_U−D_B判断。

本轮明确沿用LF11公共a0/b0，直接读取calibration.json，不在新父态或各臂重算：
C=Lobs/a0+λ(k)(5LBC+LIC)/b0；
D_B=C；P_U=C+λ(k)J_U/b0。
保留原残差尺度1/4/5、BC/IC尺度、子项与平均分母、四窗测度、λmax=0.1及200步ramp。
两臂共享观测/BC随机流和优化配方；不能给P_U单独调权或救援。

每臂上限：1000 Adam updates＋200次固定目标L-BFGS评估。
Adam默认lr=1e-4，沿用betas/eps/clipping。转L-BFGS前固定λ和观测、BC/IC、内部积分池，与独立审计池分离；不在线改变目标。
只保存预声明父状态、Adam第500步、第1000步和最终接受终点。正式裁决用固定预算终点或优化器内在停止，不按中间reference挑最好checkpoint。

五、评价与唯一条件备选
全部阶段完成、训练进程结束后再统一读取nominal参考。保留：
正式S、raw Ephi、ET/0.45、EV、EI；
双周期recall/precision/mass/timing/recovery；
两端电流、输入功率、Joule功率、能量与功率轨迹误差、current-balance和power-defect。
近零容差与参考尺度执行前冻结，严格器件门单列，不用历史floor当置信界。

匹配信号沿用：S/Ephi均改善≥10%，ET/EI/EV恶化不超过5%，近零量按原冻结绝对容差。器件结论必须同时检查底流、功率与守恒，不能只挑顶部电流。
保留强数据网络、全部直接基线；dense LF_ONLY和native solver作为更多信息/同物理参照，不混入同观测胜率。

仅当新P_U相对D_B出现相态/器件损害，且至少两个预声明节点的“残差子项×参数头”证据持续指向electric/绝缘的导电率幅值通道，才启动唯一备选。
先区分Dirichlet迹误差、phase no-flux等直接解释；大底流或单点有害梯度本身不触发。
Adam分解使用该阶段实际状态和共同分母；L-BFGS不得套用旧Adam历史。参考诊断只能服务已声明条件，不用于选checkpoint。

备选为完整可微电学块归一化：
R_V=ΔV+∇logσ·∇V；
齐次绝缘残差σ∂nV→∂nV。
保留T/phase完整坐标和参数梯度，不detach、不除以U；Dirichlet、thermal、phase及其他目标不变。

同父、同预算比较：
R：原电学块，身份一致时复用新P_U；
N：归一化块，一次参考盲起点校准匹配原块loss尺度；
G：原块乘冻结全局标量，匹配N的起点全参数梯度范数。
校准包含实际内部/BC系数和测度；不可辨就报告，不动态调常数。
G/N各1000 Adam＋200次评估。N阳性后才补同预算D_N：相同归一化绝缘BC，但没有内部PDE。
若G或D_N解释主要收益，或只有新loss下降、原始器件量变差，不立归一化独立贡献，不再串联其他救援模块。

本轮主线上限：2000 Adam＋600次完整目标/梯度评估；
含全部条件后续：5000 Adam＋1200次评估。不得超额；不同目标评估成本另记，不能视为等算力。

六、交付与科研收口
关键图同时呈现：
V续拟合的完整目标轨迹与0.5%门；
前后底流/耗散分解；
新D_B/P_U的场、双周期事件、双端电流和功率；
预声明强直接基线。尚未运行的部分不造图。

交付实际端点、接受的优化状态、配置/计数、结果表、关键图和最小复现入口；保留paper_v25，在下一版中只写实际成立的结论。
通用优化和T适配器是底座；V-only是电势作用诊断；独立方法贡献须来自匹配PDE增量及必要反事实，不包装普通调参。
首次可信阳性后停止加模块，提出两个新初始化、一个新观测规则及一个完整新协议的最小确认，另批后执行，不自动扩张。

不要以“检查通过、SHA一致、代码完成或某门失败”作为最终交付。争取正面增量；若不成立，也必须完成预算内有判别力的证据链，说明排除了什么、保留了什么，以及唯一最优先下一决策。不得保证或编造阳性，不隐藏失败。

最终汇报：新增结论、关键数字、图稿/复现位置、实例关闭状态、本轮取得的实质性研究成果和进展、当前可写进目标论文的idea/创新点/方法模块、下一步最优先解决的问题和初步拟定的研究方案。