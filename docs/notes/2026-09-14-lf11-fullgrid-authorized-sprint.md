任务：执行 PINN×PCM“全网格软电学反事实→干净初始化确认”研究冲刺，优先巩固论文主体方法证据。
工作根：E:\Python demo\PINN-PCM-SCI
继承：codex/v30-research-results，提交7f4d553f98f3117495a7072e27517886fa07709e。main仍是旧版，不能仅据main判断进展。

本指令授权下述有界实现、训练、评价与本地论文更新。简要确定实施方案后连续执行，范围内不反复请示；改变物理/观测、读取stress、超预算或新增外部权限才询问。不自动commit/push/PR。

一、执行原则
- 快速读取AGENTS、当前状态和必要源码；有更新有效成果则承接，不覆盖或重复运行。不做全仓审计、重复SHA检查、无关重构或成套治理文档。
- 保留V30：E对两个有效F/projected通过同一B层；A、严格双周期及剩余热/phase PDE独立必要性未成立。既不抹去阳性，也不夸大。
- 长训练优先平台GPU；小型接口检查用CPU。内部记录计算量，不报告GPU价格、费用、时长。每批任务完成后回收必要产物、及时关闭实例并确认，再本地读取参考评分；不空挂等分析。
- 重要选择先分析“机制—唯一改动—匹配反事实”。现阶段优先强对照和确认，不为创新数量堆模块。数值后端、显存分块可以优化；正式对照不得单独改结构、调权或救援。

二、阶段A：只新增一个F_full
输入：
paper/paper_v28/evidence/parent.pt
paper/paper_v24/evidence/input/sparse.npz
V30原冻结配置、aE/bE、观测流及calibration/lbfgs/audit池。
本地原产物优先复用。必须从原E0启动，不从F_raw终点续训。
固定参照E＝V28 P_E；保留D_E、B_E、F_raw、F_bal，复用其有效结果。V29较长训练历史只能作背景。

F_full沿用F_raw的η=1、V/T/phase和T适配器、原输出、谐和面算子、半电阻焦耳沉积、热单元与phase残差。唯一改动：在原物理时刻，将电学残差128单元采样均值改为80×40全部3200单元体积均值：

r_e,i=[A(σ)Vθ−f]_i/ω_i；
J_e,full(t)=Σ_i ω_i r_e,i² / Σ_i ω_i；
L_full=C_F+F_Tphi+λ/(3bE)·Σ_t m_t J_e,full(t)。

均匀网格即mean(r_e²)，不能仍除128、乘25或重新校准η。
保留原128单元抽样和RNG推进，继续用于T/phase；不改其采样，不在仅观测时刻新增electric loss。只识别空间积分变化，不声称匹配了全部时空约束。

沿用V30全部其他设置：aE/bE、残差尺度、BC/IC分母、λ的200步ramp到0.1、观测测度和优化流。只做全网格均值、梯度累计及原T/phase样本不变的小型检查，随后立即训练。

预算：1500 Adam＋最多300次完整固定目标/梯度L-BFGS评估，fresh optimizer。
closure固定目标、不重采样、不动态调权、不裁剪真实导数；重复和试探均计数。预算结束恢复最后接受模型/优化器。训练电学线性正解/伴随为0/0；显式全网格及AD工作如实计数。

三、阶段A评价：network/projected均保留
固定终点保留两套读出：
network使用原Vθ/Tθ/phaseθ；
projected固定同一T/phase，在160×80正式网格重求自身电学。
最多278次非零时刻正解，无推理伴随；不求新的热/phase轨迹，不增加标签。两套T/phase及事件完全一致，原network分数不覆盖。

主要比较：
F_full−F_raw：空间全积分的作用；
E−F_full/projected：E优势能否经受该强控制。
同时保留全部旧soft/projected、D_E和B_E。

沿用原A/B、10%增益、5%非劣、近零容差及严格事件语义。报告绝对误差、百分点和相对提升；未过10%不等于等效。双向非劣且无新增明显场/事件代价时，只能称该固定终点下实用近似持平；互有优劣则称取舍。

补报告性诊断：在原固定物理时刻记录采样与全网格electric residual，并按heater相邻、其他边界、内部cell分项。

阶段A结束后：
- E仍通过同一B层：限制了空间抽样解释，进入配对确认。
- F_full/projected追平或反超：接受soft＋projection为更简单待确认候选，E仍作强对照；不救援消元主张。
- 效果小或存在取舍：如实保留，不硬判等效或赢家。
- 数值无效：不算E获胜；按既有最小工程恢复规范处理，不重跑已有效训练。

四、阶段B：锁定最近强soft，再做两个干净配对
阶段A后，依据projected电流/功率的Pareto关系及原场非劣条件，从有效F_raw/F_bal/F_full中锁定最近强soft，披露这是开发阶段选择。
不能挑较弱配置。若不存在可明确锁定的单一比较者，停止扩大训练，交付已完成的阶段A及一个最小选择决策；不自动扩成多配置确认矩阵。

比较者锁定后，执行两个新初始化29、43：
不得加载任何旧训练权重。使用同一sparse和预先固定的原网络/T适配器，适配器按原零输出规则初始化。每seed先生成一个共同观测拟合父态，再分叉E和锁定soft；每对共享T/phase权重、观测及拟合历史。E初始V来自求解、soft来自原V头，继续披露这一结构差异。

共同拟合每seed上限：2400 Adam＋600次完整观测目标/梯度评估。仅使用原三场观测目标，不求电学、不读reference；建议L-BFGS按V/T/phase各200次固定分配，执行前冻结。预算结束保留接受状态、不恢复旧0.5%门，可为表现差的seed重新拟合或更换seed（最多两次）。合法但拟合较差也是确认结果；数值非法单独标记。

每seed在该E0上一次生成共同a_s/b_s，E与soft共享；保留同一物理目标、分母、调度和池规则，不逐臂调尺度。若锁定的是F_bal，沿用其预声明梯度校准规则在该seed父态算一次η；不是按参考搜索。每seed E/soft各1500 Adam＋300完整评估，fresh Adam及L-BFGS。

两个seed合计（含父态）：10800 Adam＋2400完整评估。
两个E训练正解、伴随上限各54000；校准/审计另列各≤1000；soft训练正/伴随0。
四个终点的细网格电学推理解最多1112，soft同时保留network/projected。同协议B_E可复用，不重训。

包含阶段A的全部上限：
12300 Adam＋2700完整评估；
训练电学正解/伴随各≤54000；
校准审计各≤1000；
正式推理解≤1390。
这些是上限，不一定要用满。

五、确认、科研trick与停止边界
历史seed17有不同培养流程，不与新29/43合成“三个同协议重复”。分别给出每个新seed的配对绝对/相对效应、原门及全部事件代价。两次同向支持有限的干净初始化确认，不给可靠总体成功概率或精确置信区间。反向或明显不稳时缩小主张，如果要救seed最多限两次。

本批不默认新增D_E训练；保留其旧结果及“热/phase残差独立必要性未知”。要主张该必要性时另批对应对照。不自动继续κ、hard lift、phase latent、专家、stop-gradient、V初值再拟合或热求积扫描。架构创新只有在当前比较指出明确瓶颈后，才提出一个功能清楚、带反事实的新方案。

不增加观测或参考电流/功率标签，不读取stress。新mask、完整协议、材料对象和参考生成均留待后续明确授权。新case用自身support重训称重建/适配。

六、必须交付的论文初稿相关成果
1. 有效匹配数表、接受端点/优化状态、配置与计数、最小复现入口。
2. 首图并列E、F_raw/projected、F_full/projected、D_E、B_E的绝对电流/功率及S/Ephi，连接每个F的network→projected；完整两周期事件不得隐藏。阶段B另用每seed配对效应图。
3. 保留paper_v30，更新下一版正文与主张矩阵，按“事后修复→共同投影后训练差异→全空间soft反事实→干净确认”组织，不再按历史版本堆实验流水账。
4. 成熟求解器、VJP、FV、优化器和η校准不各算原创；方法贡献由匹配增量及其适用边界支撑。

目标是取得正面且可归因的论文成果，但不得保证或编造阳性。阴性也须完成有效反事实、关键替代解释裁决和图稿收口，不能以代码完成、SHA一致或笼统“仍失败”结束，更不能无限调参。
最终汇报：新增结论、关键数字、图稿/复现位置、实例关闭状态，以及：本轮取得的实质性研究成果和进展、当前可写进目标论文的idea/创新点/方法模块、下一步最优先解决的问题和初步拟定的研究方案。