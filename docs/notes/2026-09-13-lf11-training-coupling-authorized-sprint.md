任务：执行 PINN×PCM“训练期电学耦合 vs 事后电学修复”匹配研究，优先补齐论文核心模块的独立价值。
工作根：E:\Python demo\PINN-PCM-SCI
继承版本：59e3a5a5cd4c2224f4dbaab5f16de299b0607a9d。

本指令授权下述有界实现、训练、评价和本地论文更新。先简要明确实施决策，再连续执行；范围内不反复请示。改变物理/观测、读取stress、超预算或新增外部权限时才询问。不自动commit/push/PR。

一、立即转向新的匹配问题
- 保留V29全部结论：强基线收益成立，剩余PDE独立增量未成立，旧P_F未触发。停止追加κ、同目标续训及针对周期2 timing的单门救援。
- 新协议直接研究：训练中消去电学自由度，是否优于同接口软电学PINN及其“最终重求电学”的版本？
- 新建运行目录和授权入口，复用P_F数学内核，不修改旧conditional-decision或伪造旧触发条件。
- 快速读取AGENTS、状态、V29最新评估和相关代码；已有有效材料直接复用。不做全仓审计、重复SHA检查、无关重构或成套文档。

二、共同父态和强对照
使用V28原始E0，而不是V29更充分训练的端点：
paper/paper_v28/evidence/parent.pt
paper/paper_v28/evidence/frozen-config.json
paper/paper_v28/evidence/calibration.json及原固定池
paper/paper_v24/evidence/input/sparse.npz

通过现有fit_model加载完整模型、T适配器及原V头。固定消元参照E＝已完成的V28 P_E（1500 Adam＋300评估）；同时保留D_E、E0、B_E。优先复用其有效结果，不重训。V29 D_C/P1/P_kappa仅作不同训练历史的背景。

保持物理、双脉冲、本构、IC/BC、观测、T/phase结构及原输出映射。E的初始V来自电学解，F的初始V来自父态原V头；明确披露。该比较识别训练方法包，不只隔离VJP或排除全部初值因素。不增加V拟合资格门，不先重训S0/S1。

三、软电学PINN接口与两种预声明强度
F开放V/T/phase及T适配器，三场联合训练：r_e=(A(σ)Vθ−f(U,σ))/cell_volume。
复用E相同的谐和面电导、heater overlap、半电阻焦耳沉积、热单元残差和原phase方程。电学边界通过同一面网络进入残差，不额外恢复一套不同的电BC平均；保留其余BC/IC分母13/3。F训练不解电学线性系统，但保留σ和焦耳项对全部参数的完整显式梯度，不能detach T/phase。

L_F(η)=C_F+F_Tphi+ηE_e；E_e=λ·Eρ[(r_e/1)²]/(3bE)。
C_F中的V观测使用Vθ；其他观测、热/phase尺度、aE/bE、λmax=0.1和200步ramp全部沿用V28，不逐臂重校准。

预声明两个配置：
F_raw：η=1。
F_bal：在原E0校准池、λ=0.1下，一次计算实际加权目标的五块梯度：gobs、gBC/IC、gT、gphi、ge，均对全部F可训练参数。
G0=sqrt(||gobs||²+||gBC/IC||²+||gT||²+||gphi||²)；ηbal=G0/||ge||。
比例可辨则冻结，不动态调权、不按参考选择。不可辨则记录原因，仍完成F_raw；与1数值等价时省略重复臂。缺失对照不能算消元获胜。

四、实际训练与预算
每配置1500 Adam updates＋最多300次完整固定目标/梯度L-BFGS评估。主线新增上限：3000 Adam＋600次完整评估；校准最多一次五块梯度扫描，另行计数。
fresh optimizer；学习率、betas/eps/clipping、时间组批、观测/物理流沿用V28。转L-BFGS前固定λ和原完整积分目标。closure不重采样、不动态调权、不裁剪真实梯度；正确分块累加，试探和重复评估全部计数。预算耗尽恢复最后接受模型与优化器，不保留未接受试探。保存预声明中间状态和最终接受态；正式比较只用终点，不按reference选checkpoint。个别配置无效不取消其他配置，也不能把无效当作对手失败。

长任务优先平台GPU。此次正式匹配不改架构；机制证据出来后才能提出单模块结构调整及反事实，不能为制造阳性单独救援某臂。
F训练只做网络、AD和显式面运算，不调用电学正反解；记录真实工作量，不能把两配置总预算与单个E称等算力。

五、每个F终点同时保留两套读出
1. network：原Vθ/Tθ/phiθ，直接使用共同FV读出，不修补。
2. projected：固定同一Tθ/phiθ，在160×80正式网格重求电学V†，再用同一读出。
每终点最多278次非零时刻推理正解，两配置最多556次；零驱动解析跳过。

projected只用自身σ、已知U/BC，无新标签、训练或完整热/相态轨迹求解。两套读出的T/phase/S/Ephi/事件必须相同；不把它们算成独立模型重复。
保留原network分数，不能用projected覆盖。AD/FV诊断不得替代正式指标。

全部训练和自身预测完成后回收产物，及时关闭GPU实例并确认，再本地读取高保真参考评分。不报告GPU价格、费用或时长，不空挂等待分析。stress始终不读。

六、预先冻结评价和结果路由
沿用正式S/raw Ephi/ET/EV/EI、双周期recall/precision/mass/timing/recovery、功率轨迹、能量、局部q及电学缺陷。
新方法比较沿用10%增益、5%非劣和原近零容差，A/B分别裁决，严格器件门单列。守恒、双端相等和P=UI不是独立的多条经验优势。

必须分开比较：
E对F/network：完整训练＋推理流程差异；
F/network→F/projected：固定T/phase下的推理修复；
E对F/projected：相同推理电学求解后是否仍有学习状态优势。

训练期消元增量须在同一A或B层面对两个有效F/projected都成立，不能只胜较弱配置，或把对不同配置的不同层拼成成功。
同时保留D_E/B_E与V29背景端点，披露不同训练历史。

决策：
- E只胜network，projected追平：主要优势可由事后修复解释，不立训练期消元必要性。
- F/network已经同样好或更好：优先较简单有效方法，不强留消元。
- E面对两种projected仍有可信增量：获得方法包信号，停止堆模块，进入确认计划。
- 近似持平或有明显取舍：按测试边界收口。
- 数值无效或预算未完整执行：不计为方法优胜。

七、唯一条件备选：缩小的数值敏感性检查
仅当主线未识别增量，且下一实际决策确实取决于是否修改热接口时执行；先写明两种结果各改变什么行动，不将其设为新资格门。
固定V29 P1/P_kappa两个函数、原32个audit时刻、80×40热控制体和phase位置：
比较原中点 vs 二点高斯面/2×2单元求积；80×40 vs 160×80电学源，细q体积保守聚合。
缓存复用电学解，最多128次正解，零训练、零参数梯度、零伴随；关断跳过。

比较同规则方法差值：Δ(q,h)=J(q,h,P_kappa)−J(q,h,P1)，看是否换号及差值变化，而不是仅看绝对J漂移是否超过2.19%。
稳定不证明连续体正确，不稳不证明细网格/高斯必然更准；不在该检查中继续修接口或训练。

八、必须交付的论文证据
- 完整匹配数表、端点及优化状态、冻结配置、计数、最小复现入口。
- 图：上部S/Ephi与完整事件；下部每种F的network→projected连线，并列固定E、D_E、B_E。直接回答“训练学得更好，还是最后修得更好”，不以loss或机器精度守恒作主胜点。
- 保留paper_v29，更新下一版实际方法、结果、反证和主张矩阵。热/phase残差尚未证明独立必要仍需披露；求解器、VJP、L-BFGS和η校准不各算原创。
- 阳性后另列两个从头初始化、干净新观测规则和完整新协议的最小确认，另批执行。新mask不能继承见过删减观测的权重；案例自身support重训称重建/适配。

目标是取得正面、可归因的实质性进展，但不得保证或编造阳性。即使没有实质性研究成果，也必须完成有效匹配和关键替代解释裁决，并形成可入稿图表；不以代码完成、SHA一致或笼统“仍失败”结束，不无限调参。
最终汇报：新增结论、关键数字、图稿/复现位置、实例关闭状态，以及：本轮取得的实质性研究成果和进展、当前可写进目标论文的idea/创新点/方法模块、下一步最优先解决的问题和初步拟定的研究方案。