# 观测预测保持的相态补全：授权与材料整合

任务 `PCM-20260924-OBSERVATION-PRESERVING-PHASE-01`。用户要求整合
`E:/PINN-PCM/Review_20260924.md`、`Research_Plan_20260924.md`，执行
`CODEX_Execution_Instructions_20260924.md`。执行文件是本轮具体授权；评审和规划中的事实仍须由原记录核验。

本轮 supersedes 旧关闭状态对这一新任务的限制，不重开相对残差／时间矩路线。
阶段0—2直接执行；既有GPU已只读连接确认开启（V100 32GB），故前置门通过后阶段3可直接执行。
沿用用户无费用预算及完成后回收关机的明确授权。三个分支G→N→S，共1800 Adam、最多600完整L-BFGS评估。
阶段4、新参考／case、噪声、读脉冲、T解冻、调参救援、Git发布和外部数据上传均不在本轮授权内。

本地HEAD为 `a96303c2110ae1a79984ed7dcddf0c01ab07bec1`，相对指令基线
`ac8c63686398399e0190ede5b78de5a1054be337` 仅新增导师审阅用精简稿及其生成文件；没有新的科研结果改变本任务前提。既有未提交文件保留。

论文问题：在严格零电压与既定相态缺测的交集D=(1.36,2.02)内，原热／相态PDE能否改善相态而保持基础观测预测？
基础固定为旧B1 E/29终点；N/G相同1217参数修正，S为7749系数三次B样条。
G是全时域普通修正，S是更丰富的传统表示控制；D_E29为已有强参照，不能冒充同新增成本分支。
本轮是已见短间隔协议上的开发；没有formal OOD或新基础初始化。

采用评审关于断电后多余活跃区域、短片时间矩近似点态目标、梯度与loss占比区别的判断；
不采纳“全部PDE有害”“已证明梯度冲突”“断电即无相态”或由新名称直接主张创新。
旧八臂NO_INCREMENT、B1 0/12、全标签器件收益、严格事件失败均按各自证据身份保留。

前置停止条件：用原数组判断固定D是否至少可能达到原A_w；不足则不启动训练或偷偷扩窗。
代码及真实接口有效后再固定三臂目标／梯度profile与实际电学上限。
N/S必须保持观测预测、T和端口，且不能改善原加热事件指标。所有新增终点锁定后统一评分。
物理资格按指令预先固定：独立D池raw相态平方至少下降10%，热与相态BC不超过max(基线×1.05,基线+1e-12)。
结果路由以完整原A_w和直接控制为依据，不拼接不同分支的优势。

附件所称SOURCES.md与独立sanity脚本未在本次指定路径找到。必要原始近邻由官方论文单独核对，来源与阅读范围写入工作稿；不把附件制造场当作本项目集成证据。
