任务：执行 PINN×PCM 新一轮“继续拟合—软边界—内部PDE”匹配研究，优先取得可入稿的实质增量。
工作根：E:\Python demo\PINN-PCM-SCI
继承版本：4c16ba2ece84d4dd0c561f22ff91fe5b4932cfc6。

本指令授权以下有界实现、训练、分析及本地论文更新。先简要确定关键实施决策，然后连续执行，范围内不反复请示。新增物理/观测、读取stress、突破预算或新增外部权限时才询问。不自动commit/push/PR。

一、本轮明确采用新协议
1. 不再追加V-only，不先开发hard lift，不重复S0/S1或接触归因实验。直接从paper_v26最终模型启动D_I、D_B、P_U。
2. 旧协议仍记“V未过0.5%、后续未运行”；新协议研究当前指定父状态，不声称已过原门，也不另设0.6%等替代门。通量、守恒及严格事件能力是评价目标，不再成为前置资格门。
3. 新共同父状态一次校准a*、b*，三个臂共享。此项明确替代此前“沿用旧a0/b0”的未来执行建议，但不修改旧配置和历史结果。新旧实验差异不能单独归因于拟合修复。
4. 按AGENTS读取必要上下文，快速确认有无更新结果；已有有效产物直接复用。SHA仅用于必要身份边界，不做全仓审计、重复资格化或无关重构。

二、输入、共同底座与训练trick
输入优先使用本地对应原产物：
paper/paper_v26/evidence/endpoint-with-V-optimizer.pt
paper/paper_v24/evidence/input/sparse.npz

通过现有fit_model正确加载完整模型及T适配器。三臂初始权重完全相同，V/T/phase及适配器全部联合训练；不继承V-only优化器历史。
保持原二维nominal物理、脉冲、本构、IC/BC、输出映射和观测。禁止dense权重、完整参考场、teacher电流/功率、新标签和stress进入训练。旧观测全部属于已见训练/开发信息，不重新划分为未见验证。

优先复用FP64、已有平滑残差适配器、Adam→固定目标L-BFGS等成熟trick。参数、批量和分块可按真实实现需要在正式运行前统一调整并冻结；不得看参考结果后单独救援某臂。
本轮先不改网络结构。确有机制依据的结构创新留到匹配证据之后，以单模块、明确反事实进入下一计划；不恢复已撤回的四次方hard lift，不同时堆专家、焓、phase latent、filter和采样改造。

三、三个目标与一次公共校准
在共同父状态θ*计算：
a*=Lobs(θ*)，使用全部可见观测的原精确测度；
b*=[J_U+5LBC+LIC](θ*)，使用固定无标签校准池。
计算后共同冻结，保留1e-12数值floor；不逐臂、逐头或逐阶段重校准。

令λ(k)=0.1·min(k/200,1)：
C_I=Lobs/max(a*,1e-12)+λLIC/max(b*,1e-12)；
D_I：C_I；
D_B：C_I+λ·5LBC/max(b*,1e-12)；
P_U：D_B+λJ_U/max(b*,1e-12)。

保留原观测目标、phase完整logit增量、原PDE尺度1/4/5、BC/IC子项及平均分母、四窗积分。硬满足或近零的项不通过删项改变其他分量权重。
D_I→D_B只增加软BC包；D_B→P_U只增加内部PDE。父状态→D_I/D_B均含继续优化和phase解冻，不能称纯BC效应。
D_I和D_B是无内部PDE控制，不包装成完整耦合PINN。

四、正式训练：一次完成三个匹配臂
每臂上限：
1500 Adam updates，默认lr=1e-4，沿用betas/eps/clip；
随后最多300次完整固定目标/梯度L-BFGS评估。
主线合计：4500 Adam＋900次完整评估。

三臂共享观测及适用的BC/IC批次流、初始化和优化配方；P_U按原uniform内部目标采样。沿用现有batch与时间分层，不临时改成因果训练。
转L-BFGS前固定λ=0.1、完整观测目标及BC/IC和内部积分池，独立审计池另设。优化器fresh启动。
closure不重采样、不动态调权、不裁剪真实梯度；分块累计真实积分权重。
所有初始、重复和线搜索试探评估计数；预算中断时恢复最后接受的模型与优化器，不把试探点当终点。

保存父状态、Adam500、Adam1000、Adam1500及最终接受状态。正式比较使用固定预算终点或优化器内在停止，禁止按中间reference挑最好checkpoint。
个别臂数值失败不取消其他有效臂；缺少必要对照时阻塞相应主张，不能将其算作候选获胜。

五、评价与判别：不增加无关检查
正式运行前冻结：
A. 重建增量：S和raw Ephi均改善≥10%，ET/EI/EV非劣5%，近零容差沿用LF11；严格器件门单列。
B. 功能线索：底流NRMSE和功率轨迹NRMSE均改善≥10%，S/Ephi/ET/EV/顶流误差非劣5%。事件及其他器件代价完整披露；此层不能冒充A或完整器件成功。
两个层次分别报告，不合并成“任一通过就算全面胜出”。

比较顺序：
D_B−D_I判断软BC增量；
P_U−D_B判断内部PDE增量；
再面对预声明B_logit_waveform_contact及其他强直接基线。
保留原B系列、dense LF_ONLY和native solver的不同信息角色，不混入同观测胜率。

全部方法从自身场计算正式FV双端电流与耗散，不复制teacher trace、不设I=P/U、不扣除迹项后评分。
报告正式S/Ephi/ET/EV/EI、双周期事件、底流、功率轨迹、能量、current-balance及power-defect。
复用父态和终点的heater trace、I_trace/I_drop/AD、内/顶/底耗散分解；交叉项保留符号，RMS不相加成贡献率。诊断用于解释，不替代正式指标。

仅在P_U的Adam500/1000/1500做必要的残差子项×参数头诊断：
electric、绝缘、heater Dirichlet、phase no-flux及thermal分别列出；实际共同Adam分母、历史动量和总方向分开。
不能用聚合boundary→phase宣称heater电压直接作用，也不能把局部有害分量当完整轨迹因果；L-BFGS后不用旧Adam状态解释。
诊断不参与checkpoint选择。涉及高保真探针时，训练结束后才本地计算。

六、唯一条件备选：电学块归一化
只有新P_U相对D_B产生预声明的相态/器件损害，且Adam500、1000、1500均有支持electric/绝缘导电率幅值通道的实质方向证据，才启动。
沿用已有具名幅值诊断规则并在运行前固定；若主要解释是heater迹、phase no-flux、数值/参考语义，不触发。大底流、原J下降或某一个梯度符号都不够。

令s=logσ：
原内部rV=σ[ΔV+∇s·∇V]，归一化为RV=ΔV+∇s·∇V；
原齐次绝缘rN=σ∂nV，归一化为∂nV。
保留完整T/phase坐标与参数梯度，不detach、不除以驱动U；Dirichlet、thermal、phase及其尺度不变。

从同一θ*比较：
R：原电学块，身份相同时复用本轮P_U；
N：归一化块，一次参考盲父态校准匹配原块loss尺度；
G：原块乘冻结全局标量，父态匹配N的全参数梯度范数。
校准包括实际内部/BC系数、积分测度和分母；沿用本轮a*/b*，不可辨时不动态救援。
G/N各1000 Adam＋200次评估。
N相对R/G有匹配增量后，才补同预算D_N：保留归一化绝缘BC，去掉全部内部PDE，排除仅边界重权解释。

G或D_N复现主要收益、只降新loss而损害原始功能量、或梯度实现不等价，均否定对应方法主张。
全部条件路径上限不要求用满，不再自动串联第二备选。

七、运行边界与结果收口
不报告GPU价格、费用或时长。内部保留真实计算统计，不同目标评估不视为等算力。
每批GPU任务结束立即回收必要产物、终止进程、关闭实例并确认，再本地读取nominal参考；不能空挂等分析。CPU-only如实记录，无须借用旧关机证明。stress始终不读。

结果路由：
- D_B有效、P_U无增量：保留BC收益，不强行发明hard lift或归一化贡献。
- P_U有效：停止加模块，优先最近强方法对照、关键反事实和确认。
- 只有功能线索：按该层准确收口，不包装为相态重建成功。
- 全部无增量：完成三臂差分及必要机制判别，明确下一步应处理哪个接口，不重复返回“PINN仍失败”或继续相同目标无界训练。

八、必须形成的研究交付
1. 完整匹配结果表、固定端点、接受优化状态、配置/计数和最小复现入口。
2. 关键图：同父D_I/D_B/P_U的场与双周期事件，双端电流/输入及Joule功率，接触迹/增量和内部耗散；并列父态及B_logit_waveform_contact。不能只画loss曲线。
3. 保留paper_v26，更新下一版论文和主张矩阵：哪些是通用拟合底座、哪些是边界诊断、哪些是独立PDE/方法增量，严格分开。
4. 首次可信阳性后，另列最近强对照、两个新初始化、一个新mask和完整新协议的最小确认。

争取正面方法成果，但不得编造阳性、弱化基线、移动结果门或隐去失败。实质进展应至少包含完成的有效匹配比较、一个被支持或排除的机制解释及可直接入稿的图表，不以代码完成、SHA一致或资格检查代替。

最终只汇报：新增结论、关键数字、图稿/复现位置、实例关闭状态，以及：本轮取得的实质性研究成果和进展、当前可写进目标论文的idea/创新点/方法模块、下一步最优先解决的问题和初步拟定的研究方案。