任务：执行 PINN×PCM“完整新脉冲历史确认”冲刺，锁定已有方法，尽快补齐跨案例证据并形成下一版论文。
工作根：E:\Python demo\PINN-PCM-SCI
继承：codex/v31-research-results@c347b3f561c033271d2e480fa62191d68cb42870；不能仅查看旧main。

本指令授权以下新case生成、参考求解、有界训练、评价及本地论文更新。简要确定实施方案后连续执行，范围内不反复请示。改变其他物理、增加观测、读取stress、超预算或新增外部权限时才询问。不自动commit/push/PR。

一、研究目标与执行原则
- 保留V31：F_full与两个干净配对均已完成；E对锁定soft的B层有两组支持，对B_E及严格事件尚不全面成立。
- 本轮只问：缩短双脉冲间隔后，E能否继续超过F_raw/projected和同求解层B_E？不重跑旧实验、不救seed43、不追单一timing门。
- 快速读取AGENTS、状态和必要源码，确认有无更新有效结果；不做全仓审计、重复SHA检查、无关重构或成套治理文档。
- 长训练优先平台GPU，CPU承担适合的参考求解和小型检查。不报告GPU价格、费用或时长；内部记录计算量。每批任务完成后回收必要产物、及时关闭实例并确认，再本地读取参考评分，不空挂等分析。
- 重要改动先分析“机制—改动—反事实”。本轮是确认实验，锁定网络与科学配方。

二、唯一新物理因素：有限双脉冲间隔
新建独立case配置，不覆盖旧合同。保持空间域、材料、本构、几何、IC/BC、总时域[0,2.5]和单脉冲形状。

定义单脉冲s(t)：
0<t<0.05：t/0.05；
0.05≤t≤0.27：1；
0.27<t<0.35：(0.35−t)/0.08；
其余为0。

新驱动：
U(t)=0.72[s(t)+s(t−1.01)]。
恰好两个脉冲，禁止无限周期取模导致第三个脉冲。
第二脉冲由1.25提前到1.01，位移0.24等于12个原稀疏时间间隔0.02；保持观测采样相位、总时域、时间归一化与startup。

四物理窗口：
[0,0.35]、[0.35,1.01]、[1.01,1.36]、[1.36,2.5]；
积分质量：0.14、0.264、0.14、0.456。
支持指标使用两个加热窗；恢复分别按[0,1.01]和[1.01,2.02]，其余尾段另报，不借额外尾段放宽第二周期。
reference、网络、采样、B_E和评价统一读取此case配置，避免局部仍调用旧周期函数。

三、生成新支持与固定参考
复用原数值算法、容差、迭代上限和不裁剪规则，仅运行：

1. 支持载体：80×40，积分dt=0.0025，save_every=2，1000主时间步。
2. 评价参考：160×80，积分dt=0.000625，save_every=4，4000主时间步。
主线共两条轨迹、5000主时间步。保存间隔不能当积分步长。
内部电/热线性解及phase Newton另计；执行前依据原调用路径和迭代上限一次展开预算，不能把一时间步记成一次正解。

从支持载体只导出原空间mask及t=0,0.02,…,2.50，保持21×11×126，解析IC单列。完整载体、参考场、电流/功率和stress不得进入训练进程。参考生成与模型训练分离；只做新接口必要检查：有限双脉冲、窗口质量、mask、数值合法性，以及第二脉冲开始前与旧同分辨率轨迹的因果相容性。

没有第二次独立事件不等于数值失败：保留case，报告真实事件存在性、假事件及适用指标，不换更有利间隔。

四、两个新case配对：E/F_raw/B_E
seed固定29、43。可复用明确零更新的随机initial权重以保持跨协议配对，不能加载任何旧训练父态、optimizer或校准量。原V/T/phase结构、零输出T适配器与合法输出保持。

每seed：
1. 仅用新support，共同父态2400 Adam＋600次完整观测目标/梯度评估，V/T/phase各200。
2. 从同一父态分叉E、F_raw，各1500 Adam＋300完整评估。
3. 在该父态一次计算a_{s,c}/b_{s,c}，两臂共享冻结。不设旧V拟合门，不按reference选父态。

E保留完整隐式电学VJP、半电阻焦耳沉积、热单元平衡及phase残差。
F_raw固定η=1、128电学采样单元、原显式面算子与相同热/phase接口，保留network/projected；不重选F_bal/F_full。
两臂共同观测/BC/IC、原尺度与分母3/13、λmax=0.1和200步ramp、优化配方及配对随机流不变。物理时刻按新窗口和质量生成；不同case池不伪称逐点相同。

B_E仅由新support生成T/phase插值，再使用同一电学层；作为无seed共享基线计算一次。不得复用旧case的场、曲线或评分。
本轮不增加D_E、新权重、专家、hard lift、latent或其他方法分支。

L-BFGS前固定完整观测与积分池、λ和目标；fresh optimizer，closure不重采样、不动态调权、不裁剪真实导数。
所有重复和试探计数；预算中断恢复最后接受模型及优化器，不保留试探点。

五、预算与推理
两seed共同拟合：4800 Adam＋1200完整评估。
四主臂：6000 Adam＋1200完整评估。
训练总计：10800 Adam＋2400完整评估。
两个E训练正解/伴随分别≤54000；校准和审计另列，各≤1000。F训练线性正/伴随0，显式工作另记。
四个神经终点＋一个B_E的正式电学推理解预计≤1390次非零正解；执行前一次核对新波形实际非零时刻，零驱动解析跳过。参考轨迹求解另账。

正式推理使用原160×80、1001时刻。E、F/projected、B_E均在自身T/phase上重求电学；F/network同时保留。两套F的T/phase、相态及事件逐项相同，不把它们算成独立模型。
不复制teacher trace，不用I=P/U替代独立读出，不称细网格重求解为免费后处理、加速或零样本泛化。

六、评价、停止与唯一条件备选
分别裁决E−F_raw/projected和E−B_E，不能用前者成功掩盖后者不足。
保留原A/B、10%增益、5%非劣、近零容差、正式S/raw Ephi/ET/EV/EI及严格双周期语义；9.x%不四舍五入成10%，未过门不等于等效。
每seed单列绝对误差、百分点、相对变化和完整事件，不用均值遮盖反向。

追加report-only：
- 第二脉冲开始前的ROI温度/相态及误差；
- 每个脉冲的功率轨迹NRMSE、带符号和绝对能量偏差；
- 第二周期的独立场/事件/器件量；
- 同seed跨协议的E/F误差比与差值。

结果路由：
- 两seed对F保留B且对B_E有一致实用优势：进入成稿，不继续加模块。
- 对F有效、对B_E不足：收窄为约束实施方式的有限收益，不换弱基线。
- 出现反向、事件代价或无事件：保留协议依赖，不救case、seed或阈值。
- 数值无效：按最小工程恢复处理具体问题；不得静默改科学配置。

唯一条件备选是参考时间步敏感性，不是第二条训练救援：
只有主裁决靠近方法排序/原门、与已观察数值差异同量级，且下一决策确实取决于时间离散时，额外运行一次160×80、dt=0.0003125、save_every=8的参考（8000主时间步）。
触发前记录疑点及两种结果各改变什么判断；保持全部模型、输出与参数冻结，重评分，不重训、不挑有利参考、不再追加加密。
主参考和敏感性结果同时保留；该检查不证明空间收敛。若疑点主要是空间离散，限制主张并另提方案，不自动扩预算。

七、必须交付的论文成果
1. 两个完整新case配对的固定端点、优化状态、配置/求解计数、全部指标及最小复现入口。
2. 关键图并列两个协议×两个seed的E/F/B_E绝对电流、功率与相态误差，附配对效应；用脉冲前状态和分周期功率解释物理历史，完整事件代价单列。
3. 保留paper_v31，在下一版按“同状态修复→共同投影后的训练收益→全空间反事实→干净配对→完整新协议”组织正文，不按版本堆实验流水账。
4. 求解器、VJP、FV、优化器不各算原创；主张由匹配增量支撑。D_E强、剩余PDE必要性未知、严格事件未全过等历史结论不得删除。

新case使用自身support重训只能称重建/适配；两个case×两个seed不是四个独立case，不夸大统计证据。

目标是取得可归因的正面成果；不保证或编造阳性。即使反证，也必须完成有效匹配、物理历史分析与图稿收口，不能以代码完成、SHA一致或笼统失败结束，不无限调参。
最终汇报：新增结论、关键数字、图稿/复现位置、实例关闭状态，以及：本轮取得的实质性研究成果和进展、当前可写进目标论文的idea/创新点/方法模块、下一步最优先解决的问题和初步拟定的研究方案。