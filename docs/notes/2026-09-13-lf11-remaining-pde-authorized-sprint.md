任务：执行 PINN×PCM“剩余PDE实际影响与固定目标反事实”冲刺，保住V28强基线收益，尽快补齐内部PDE独立增量。
工作根：E:\Python demo\PINN-PCM-SCI
继承版本：4bd28c126911ae521b0949b5bd59aa56d01388cd。

本指令授权以下有限诊断、训练、评价和本地论文更新。简要确定实施方案后连续执行，范围内不反复请示。改变物理/观测、读取stress、超预算或新增权限时才询问。不自动commit/push/PR。

一、承接与目标
- 快速读取AGENTS、当前状态和必要源码；有新有效结果则承接。不重复全仓/SHA审计，不重跑历史拟合、电学接口检查或V28。
- 正确保留V28结论：D_E/P_E均胜同电学层B_E，但P_E未胜D_E，独立内部残差也未更低；P_F未运行。不能说成全无阳性或剩余PDE已有效。
- 本轮唯一科学变量是剩余热/相态PDE的加入及一个冻结强度倍数，不再同时改网络、边界、观测测度和采样。
- 步长多、耗时久的PINN研究任务优先使用平台GPU，保留相容稀疏求解后端和完整梯度。不报告GPU价格、费用、时长；每批任务完成后回收产物、及时关闭实例并确认，再本地读取参考评分，不空挂等分析。

二、共同父态与不变接口
使用paper/paper_v28/evidence/D_E/checkpoint.pt以及V28原sparse输入、calibration.json、calibration/lbfgs/audit池。本地原产物可直接复用。

正确加载T/phase头和T适配器，V头继续不训练；保留电学消元、完整一阶隐式VJP、半电阻焦耳沉积、热单元平衡和原phase残差。不新增标签、teacher电流/功率、完整参考场或stress训练信息。旧观测是已见开发数据，不重新划切片冒称未见验证。

固定λ=0.1，不重新ramp，不重算aE/bE：
C=Lobs/max(aE,1e−12)+0.1(5LBC+LIC)/max(bE,1e−12)；
F=0.1J_Tphi/max(bE,1e−12)。
保留尺度4/5、平均分母3/13、原观测与积分测度，不同时降低BC或改变phase监督。

三、一次最小诊断，直接决定是否增加Pκ
先复用已有审计结果，补E0/D_E/P_E在原固定L-BFGS池上的同一C+F分项比较。
区分：训练池和审计池都较差还是仅审计池较差。不用独立审计值说明代码错误。

仅对E0和D_E，在原校准池做加权观测、BC、热、phase四块的完整一阶梯度，按T头（含适配器）和phase头分列范数、夹角与局部方向。
必须包含θ→σ→V→q的间接梯度，不能冻结q后冒称实际梯度。
最多8次完整分项梯度扫描；诊断电学正解/伴随各≤600。不做全NTK、Hessian/NNCG或全历史节点扫描；当前自定义VJP不能默认支持二阶反传。
本轮不用Adam，不移用历史Adam分母和动量解释新训练。

在D_E父态计算：
gobs=∇(Lobs/aE)；
gBC=∇[0.1(5LBC+LIC)/bE]；
gF=∇F；
G=sqrt(||gobs||²+||gBC||²)。

只有比例可辨且||gF||/G<0.1，才启用κ=min(100,0.1G/||gF||)。
一次计算后冻结，不按参考或终点调κ；达到上限如实记录。
梯度不弱、比例不可辨或κ≈1时省略Pκ，D_C/P1仍执行，不增加资格门。
若训练/审计差异显示明显积分敏感性，仅允许预算内一次冻结函数的面求积检查；不据此静默换算子。标量loss小本身不是增权依据。

四、正式同父反事实：零新增Adam
D_C：最小化C，排除继续优化本身；
P1：最小化C+F，排除换父态/固定目标优化本身；
Pκ：最小化C+κF，仅满足上述条件时运行。

三臂从同一D_E父态启动，各自fresh L-BFGS，不继承任何旧曲率历史。每臂最多300次完整固定目标/梯度评估；主线最多900次、零Adam。
使用原固定L-BFGS池和全部可见观测。closure不重采样、不动态调权、不裁剪真实梯度；按原权重分块累加。
初始、重复和所有线搜索试探均计数；预算中断回滚到最后接受模型及优化器。终点为固定预算末或优化器内在停止，禁止按参考或中途指标挑最好checkpoint。
不追加第二轮300，也不将省略Pκ节约的额度转给其他臂。

训练电学正解/伴随上限为30,000次；按现有池预计三臂约26,800对，仅核对一次实际时刻数。校准诊断另计；三个新端点细网格推理解最多834次，零驱动解析跳过。GPU分块和数值后端可按实现需要统一优化，但不得改变目标、精度或截断梯度。成熟优化trick是底座，不算新增创新。

五、固定终点评价与决策
训练完成并关闭实例后，统一生成/读取正式评价；沿用160×80自身T/phase重求电学的既定规则，计入推理解，不称免费后处理或零样本泛化。
复用旧E0/D_E/P_E/B_E指标，保留其他直接基线和native参照身份，不重训。

每个新端点同时报告：
各自训练目标、共同C+F、原热/phase/J分量，在训练池和独立审计池分别列出；
正式S/raw Ephi/ET/EV/EI；
双周期recall/precision/mass/timing/recovery；
功率轨迹、能量、局部q及电学一致性。
求解器保证的双端平衡不能重复计成多个独立方法优势。

沿用V28的A/B、10%增益、5%非劣及原近零容差，严格器件门另列：
P1/Pκ先对D_C裁决，再对B_E及原D_E核对对应层非劣。
不能靠击败继续训练后退化的D_C，宣称超过原有最好证据。
若要归因κ，必须额外比较Pκ−P1；P1已足够就优先保留简单P1。
能量单项改善不能替代功率轨迹；残差下降不能替代预测增量。

分支收口：
- P1有效：归因于新父态上的物理训练，κ不是必要模块。
- Pκ额外有效：支持本次固定强度调整。
- 训练残差降、独立审计不降：不称可推广的物理改善。
- 共同残差降但事件/器件无收益：明确PDE已参与却未提供所需预测增量。
- 全部无增量：结束此配方，不重启hard lift、旧电学归一化、phase latent或专家队列。

六、后续方法分支与创新边界
只有新P1/Pκ对D_C通过A或B，并保留对B_E及原D_E的对应层竞争力，才将P_F列为下一方法实验：同父T/phase、同焦耳/热接口和已选权重，V由网络预测并用同面残差软约束，最终不经求解器修补。
本轮不自动执行P_F。交付其最小预算和V初值处理方案，建议总计1500 Adam＋300次完整评估；必要V预拟合从该额度内划拨，不隐藏起点成本。无信号则不提出P_F救援，不自动减少观测制造有利任务。

大胆探索须有功能依据：先明确哪条机制被当前反事实否定，再提出一个结构/参数改动及匹配控制。电学求解、FV、隐式微分、L-BFGS及κ调权本身是已有方法。论文创新应来自必要接口、独立增量和器件后果，而不是名字数量。

七、必须交付
1. 完整匹配结果表、固定端点/接受优化状态、配置、评估及正反解计数、最小复现入口。
2. 关键图：旧E0/D_E/P_E/B_E与新D_C/P1/Pκ并列；共同训练/审计目标和完整梯度作用，连接正式场、双周期事件及电流/功率结果，不只画loss。
3. 保留paper_v28，在下一版中更新实际结果、方法接口和主张矩阵；明确强基线收益、剩余PDE增量、消元必要性与独立确认的不同状态。
4. 首次可信信号后停止加模块，规划P_F、两套从头初始化、干净新观测规则和完整新协议。

目标是取得正面、可写的匹配进展，不保证或编造成果。即便无实质性成果也必须完成预算内反事实、关键机制判别及图稿收口，不能以“检查通过、代码完成、某门失败”结束。
最终汇报：新增结论、关键数字、图稿/复现位置、实例关闭状态，以及：本轮取得的实质性研究成果和进展、当前可写进目标论文的idea/创新点/方法模块、下一步最优先解决的问题和初步拟定的研究方案。