# 2026-08-27 精选 GitHub 同步记录

## 状态

- `execution_status`: `COMPLETED`
- `authorization_status`: `ONE_TIME_SELECTED_GITHUB_SYNC_CONSUMED_AND_CLOSED`
- `remote`: `https://github.com/ghy001122/PINN-PCM-SCI.git`
- `branch`: `main`
- `snapshot_commit`: `0cd40eb`
- `remote_history_merge_commit`: `cad644c`
- `force_push`: `false`
- `pull_request_or_release`: `false`

## 用户授权

用户明确批准将 182 个精选变更文件推送到指定仓库的 `main`，并明确接受该仓库当前或未来公开时，相关研究文档、代码、论文和数值证据的外部披露风险。该授权只覆盖本次精选同步，不延伸为后续 push、PR、release、投稿或其他外部上传授权。

## 已同步范围

- 当前权威与上下文入口：`README.md`、`CONTEXT.md`、`active_phase.md`、`PROJECT_STATE.md`、唯一 live plan 与文档地图；
- 直接支撑当前终点的 ADR、来源审查、治理记录、历史计划和研究笔记；
- append-only 实验索引、对应 intent/manifest/claim 载体和关键终局报告；
- `GOAL-PAPER-ONE-SHOT-V1` 的 S0/S2 机器合同、SYN-EDT 实现、runner、evaluator、ledger 修改、固定依赖与 50 项关键测试；
- 完整 `paper/` 包；
- 两个 Q-only freeze manifest，以及 Q0 的 HDF5、evaluation NPZ 和 report 三个关键数值载体。

首次推送前，实际暂存区为 182 个变更文件、工作区文件总大小约 13.8 MiB。敏感模式扫描无命中；文档一致性、实验 ledger、论文 32 项包清单均通过，关键测试 `50/50` 通过。

## 明确排除

- 未选择的旧 EAF/ETPF/R1 实现、配置与测试；
- `ideaspark_run/`、缓存、虚拟环境和无关运行输出；
- 商业 COMSOL `.mph` 原始资产；
- 凭据、令牌、环境密钥、作者联系、投稿材料或付费服务信息。

## 证据边界

本次动作是工程同步事实，只为后续 ChatGPT 或协作者读取当前研究进程与成果。它不改变 `SYN_EDT_2D_V1_NUMERICAL_CONTRACT_NO_GO_NO_ORACLE_EVENT_OR_METHOD_EVIDENCE`，不建立 oracle、event、PINN、GPU、formal、实验验证、SOTA 或期刊接收主张。
