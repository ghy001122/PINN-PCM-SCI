# Revision Roadmap：具体实施方案

2026-09-17。用户当前指令：“请逐一按 Revision Roadmap 的优先级改进论文初稿。请先制定具体可行的实施方案，然后直接连续执行。”本方案将首要未完成的数值补证具体化为下述两条空间参考；旧审核、原稿和时间参考结果不改写。

**执行状态更正：**自动审批审核随后拒绝将 active_phase／PROJECT_STATE／NEXT_ACTIONS／README 的科研执行授权切换为 true，理由是本次论文改进指令未被认定为明确授权这项持久化状态变更。该操作未执行，参考求解未启动。以下预算与实现先准备到可直接审阅和运行的状态；空间求解保持 `PROPOSED_AWAITING_EXPLICIT_APPROVAL`，不绕过拒绝。文本修订、既有数据分析和不求解的映射接口检查继续完成。

## 已消除授权阻塞

2026-09-17，用户已针对上述具体预算与状态更新回复“我批准授权，继续执行”。空间参考状态现为 `AUTHORIZED_EXECUTING`，原自动拒绝保留为历史；不再重复请求同一批准。以下冻结科学设置和停止条件保持原样。

## 执行顺序

1. **R3/R5、R4/R7/R8、R1、R2、R11：承接已完成修订。** 上轮已补齐数值算法、数学条件、比较单位、最近邻、正文组织和 PDF。只在本轮新证据影响相应表述时修改，不为重做检查而重做。
2. **R6/P1：固定预测的空间参考敏感性。** 固定全部 16 套已有数组，保留四组历史 E/F、两协议 B_E 和六个相态头延续。只新生成两个既有协议的空间加密参考，以回答主要器件差距及事件裁决对所测参考改变是否稳定。
3. **R7/R10：据新参考报告完整反证。** 保留所有历史 A/B、strict、漏检/多检质量与不利指标。补证只扩展所测参考范围，不用它推断 PDE 必要性或孤立 VJP 因果。
4. **R9：完成通用作者审阅稿。** 未给定目标期刊时采用通用计算方法格式。作者身份、基金、利益声明和最终责任不能代填；公开发布仍待明确指令。
5. **更新正文、补充、图表和逐项响应，构建并检查 PDF。** 新稿使用 `paper/paper_revision_20260917/`，保留已经交付的 `paper_revision_20260916`。

## 唯一新数值实验（执行前冻结）

**问题与唯一变化：**两个协议分别从现有 160×80、dt=0.0003125 的参考，改变为空间网格 240×120；dt、保存时刻、数值算法、容差、所有物理系数、接触几何与有限双脉冲不变。空间单元数增加到 2.25 倍；这是一次空间扰动，不足以建立收敛阶或连续体真值。heater 端点在两网格都落于相容面位置。

**输入：**`outputs/submission-archive-20260916/manifest.json` 中固定预测与旧/时间细化参考；原物理合同；两协议有限驱动。模型权重、观测、超参数、原评分阈值均不变。零训练、零 checkpoint 前反向、零新方法或材料、stress 不读。

**预算：**两个新参考各 8000 主时间步（t=0…2.5，save_every=8），合计 16000 步。每条电/热/phase 内部线性求解合计硬上限 200000，合计至多 400000；实际分类计数。沿用耦合块最多 30、phase Newton 最多 30、步长线搜索到 2^-20 及原容差，不自适应改算法、dt、case 或阈值。两条运行可分别在本地 CPU 进程执行；本轮不需租用或启动 GPU。超过 cap、非有限、越界、原求解条件不满足即停止该条，保存失败证据，不追加第三参考救援。

**共同评分与参考映射：**已有预测及其 160×80 电学读出完全固定。用源/目标单元的精确重叠面积构造非负、行和为 1 的保守限制矩阵，将 240×120 参考的 V/T/phase 与局部 q 映射到原 160×80 评分单元；不插值或重推模型。新参考电流和功率保留其原生 240×120 结果，不能从已映射电势重新计算来掩盖接触差异。已有各模型的同一电学读出用于所有参考。

这一选择比上一版“全部状态换新推理网格”更直接隔离参考扰动，省去重推模型和新增投影解。相应结论只覆盖**固定预测/读出下、带预声明保守限制的空间参考检查**；不证明模型电学推理网格独立，也不将映射后的热/phase 参考称为另一条准确满足粗网格方程的解。

**阈值映射反事实：**主评分继续对映射后的 phase 作原 0.5 阈值，不悄悄改 A/B。另将细网格 active indicator 限制到粗网格，记录与“先限制 phase 后阈值”的差异质量；报告 S 的两种诊断及其绝对差上界、每周期支持质量/recall 差异。后者只作映射敏感性诊断，不替换正式评价或重新选择有利规则。不能据映射后的严格通过声称原生细网格严格能力。

**必要检查：**新限制算子的常数保持、同网格恒等、体积积分保持及阈值差界；两协议新参考在第二脉冲开始前的相同因果前缀；固定预测评分在基准参考上与既有记录一致。检查针对本轮新接口，不重跑已验证电学 VJP 或历史训练。

**结果与停止：**全部有效端点按原 S、Ephi、ET、EV、顶/底流、功率、能量、局部 q、事件、A/B、strict 完整输出。四个 E/F 配对与各协议 B_E 保持原角色。不用时空点、16 个评分对象或多指标制造独立样本数。

- 方向与门均保留：可写“所测空间参考扰动下保留”；仍披露映射和共源算法。
- 方向保留但门翻转：收窄门槛稳健性，保留有界效应。
- 方向反转：报告哪些量、哪些配对受影响，优先检查参考/读出定义，不重训救回。
- 参考不合法：不以缺失对照判方法成功，完成对应失败定位和局限修订。

首张新图并列两个协议、两个 seed 的器件优势裕量及参考原生电流/功率差；补图保留 S、strict 与阈值映射差异。结束后保存可重算的映射参考、分数、曲线和配置，更新新稿及 Roadmap 逐项响应。

## 不因本轮而自动升级的任务

R10 的剩余 PDE 必要性仅在论文需要这一新主张时立项；既有稿不作必要性主张。稳定严格事件不能由换参考替代新的机制证据。具名氧化物材料标定涉及新物理对象与验证数据，不在本次空间补证中引入。三者继续列为条件研究，不把“暂缓”写为“已解决”。
