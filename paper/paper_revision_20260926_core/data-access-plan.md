# 完整数据访问方案

**当前状态：本地完整复算包；外部访问尚未落实，P03 保持 OPEN。** 科学执行阶段未包含 Git 发布、数据上传或投稿。用户随后另行授权精简成果 Git 提交，见[发布记录](../../docs/notes/2026-09-26-core-revision-vo2-bridge-release.md)；完整数据包仍未对外上传，未创建 DOI、外部账户记录或审稿链接。

本地交付根为 `D:\Temp\PINN-PCM-Standalone-20260926`。`input-inventory.json` 列出固定输入，`recompute.py --fresh` 在包内新建干净执行目录，`independent-verification.json` 保留实际独立运行结果。原仓库在复算过程中被拒绝访问。该交付不依赖旧工作区作为备用数据源。

拟采用一个固定版本的 Zenodo 数据记录保存本项目可再分发的合成数组、完整评分定义、必要源代码和独立复算记录。正式发布前，可由作者审查元数据与文件，并使用仅有预览权限的草稿链接进行可访问性验证；Zenodo 官方说明支持草稿预览链接及到期设置。[官方链接共享说明](https://help.zenodo.org/docs/share/link-sharing/)

具体执行顺序留待发布审批：

1. 锁定此次候选与比较者清单，把新 F_cov、完整历史数组、S21/S22、S23 缓存和保存残差记录放入同一版本；不只上传有利子集。打包时排除可重新生成的临时 NPY 存储和旧执行缓存，保留必要输入与期望记录。
2. 按实际权利分别列出项目代码、项目合成数据、作者 MIT 模型代码和第三方文献资产。相关作者仓库中的实验 CSV 与出版社 PDF 本轮不进入该合成数据发布包；它们保留官方来源链接和独立许可状态。
3. 作者批准后创建数据草稿，填入真实作者、版本、方法范围、依赖与引用；可预留数据记录自己的 DOI。预留不等于已经公开，且不能拿论文 DOI 代替数据 DOI。[官方 DOI 说明](https://help.zenodo.org/docs/deposit/describe-records/reserve-doi/)
4. 先用仅预览权限分享给指定审查者；由另一台可隔离环境下载、解包并执行 `--fresh`，核对缺失输入会报错、评分和布尔判决一致、无原仓库回退。此时记录真实访问链接、验证日期与范围，而不是只报告本地存在。
5. 审查通过后，再按作者批准的公开／受限访问方式发布并把真实数据 DOI、版本、访问条件写回稿件和证据映射。只有这一步的外部获取证据闭合，才评估关闭 P03。访问链接中断、缺文件或只有残差聚合能力时，保持相应限制，不写成完整网络 AD 再现。

以上是具体的待批准发布路径；本轮完成的是完整本地输入和实际独立复算，二者不替代外部访问闭合。
