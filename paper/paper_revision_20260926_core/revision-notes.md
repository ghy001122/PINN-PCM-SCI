# 本轮修订与证据边界

任务 `PCM-20260926-CORE-REVISION-VO2-BRIDGE-01`，基线 `90508f05a6f233a485413c7589cc74420015cbea`。本稿是新的独立修订目录，不覆盖历史稿件、原评分或不利结果。

## 稿件修改

正文继续围绕电学接口适配、共同电学后处理后的比较，以及逐方程失效诊断。正文 4.3 与补充 S24 专门交代新增混合测度控制；其实际结果同时进入摘要和结论，不只放在补充表中。完整连续指标及 A/B 判决都保留，十二条敏感性记录只对应两个新增初始化。E/F_cov 的 A、B 均通过 12/12；F_cov/F 均为 0/12，但其端口误差连续改善与相态集合误差上升相邻呈现，未把完整判决失败写成所有指标无改善。

S21 从实际源码补齐八臂的共同父态、目标、系数、校准、Legendre 矩和积分规则；S22 补齐 G/N/S 的门函数、参数化、损失及独立资格。历史 CPU 执行偏差与失败判决保留。十二项 B1 检查、八臂共享父态和六臂续训均不被改写成独立重复。

S23 及其正文定位把相态 RMS 下降、活跃支持召回率代价、接缝和 D-only 80×40 测度相邻呈现。原参考窗口、事件阈值和严格双周期判决不因这一新口径改善而替换。原诊断推荐保留为历史研究建议，不自动授权 A+。

空间相态、温度和局部热源图保留；本轮增加两初始化的同尺度物理面板。五工况 VO2 的 V/I/T/R 图独立呈现，避免把作者集总模型附加为旧合成模型的材料标定。

## 复算与构建

完整本地复算包使用独立目录和独立 Python 环境，拒绝访问原仓库。旧指标公式、数值容差和精确布尔判决保持不变。缺失输入检查会立即报错。S23 只由保存轨迹和缓存重建；保存残差的重新聚合不冒充重新计算神经 AD。新 F_cov 评分在两终点锁定后完成，再用独立数组入口重算核对。

权威文本为 `source/manuscript.md` 和 `source/supplement.md`。根目录 Markdown 是包含表格、参考文献后的导出；同一解析结果生成 DOCX，再由 Word 导出 PDF。公式的可编辑来源在 Markdown，DOCX 中为嵌入公式图片。实际依赖与渲染器限制见 `build-dependencies.json`，最终逐页检查记录见 `build/visual-review.json`。没有维护三套独立正文。

已回收两条有效训练终点及四组完整读出，并在核验回收包后关闭实例。首次零更新部署故障、SSH 监测断线和盘满时的最后读出中断分别记录；两条训练没有重开，读出从提交边界恢复并另记至多一次在途电学求解。

## 文献模型与下一研究

五工况各完成 1 ns、0.5 ns 的作者代码形式复算。保存同层 V/T/R/I 和完整历史，零噪声、独立初态、Euler 顺序及原裁剪／反转规则不变。未对齐波形差、峰时、峰高和末态照报；“无检测峰”不等于平衡。正式论文 Fig.2C 的两个初始尖峰与本轮三个瞬态峰的差异没有通过改阈值或追图拟合消除。

来源审查进一步区分 Qiu 前作与固定代码仓库对应的后续 Collective dynamics 论文：公开 CSV 包属于后者的 Fig.1/Fig.5，不直接充当 Qiu Fig.2 原图数据。后续图件字母与原文件 1E3/1E2 身份的对应仍需核验，留出协议继续按原文件设备 ID 固定。此次只修正来源与计划说明，未改变五工况、模型参数或冻结离散规则。

50 Ω 测量支路、低阈值双器件参数、实验初态和二维热边界仍为 UNKNOWN。下一研究只规划有限观测离线重构，明确传统强基线和完整协议留出；未运行新二维参考、新 PINN 或参数反演。

## 仍开放的事项

P02 方法增量、严格双周期、真实材料验证与泛化限制仍开放。完整数组已形成可移交的本地包，但尚无经核验的外部访问链接或 DOI，因此 P03 保持 OPEN。Git 发布、数据公开和投稿不在本轮执行范围。
