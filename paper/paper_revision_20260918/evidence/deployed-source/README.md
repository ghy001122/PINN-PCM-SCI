# Frozen deployed scientific entry

`pinn_pcm_sci/phk_v23_readout_clean_pde.py` is copied unchanged from the executed input archive. Byte-identical to the current repository entry: `true`. The full runtime (including all frozen inputs and dependencies) remains in the local array package as `training/new-execution-runtime.tar.gz`; this directory does not replace it. Reproduction instructions and scope are in ../../data-and-reproduction.md. No training or inference is invoked by this publication.
