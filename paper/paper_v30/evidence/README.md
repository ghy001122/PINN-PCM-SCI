# Selected V30 evidence

Actual terminal: **VALID_TRAINING_COUPLING_INCREMENT_OVER_POSTHOC_CONTROLS**. Publication of this selected package was separately authorized on 2026-09-14; the verified release commit is supplied with the [handoff](../../../docs/notes/2026-09-14-lf11-v30-results-cloud-review-handoff.md). Source run: `outputs/runs/20260913-lf11-training-coupling`. Saved runtime publication flags describe their capture time and are not rewritten.

1. [Terminal](terminal-summary.json), [frozen configuration](frozen-config.json), [five-block calibration and parameter layout](eta-calibration.json), full gradient vectors in `five-block-gradients.npz`.
2. F_raw/F_bal final checkpoints contain accepted model and optimizer states. Per-arm Adam/L-BFGS telemetry and terminal reasons retain actual updates, all complete evaluations, accepted steps and explicit work. The three intermediate Adam checkpoints remain in the original local run.
3. Each role's `network/own-readout.npz` and `projected/own-readout.npz` preserve both readouts, with `paired-readout-identity.json` confirming one learned state. The compact package omits full prediction arrays and the full reference, not their separate scores.
4. [Full metrics](evaluation/results.json), [method decision](evaluation/method-decision.json), compact trajectories and reference-peak snapshots. E0/D_E/P_E/B_E are reused V28 results; V29 roles are explicitly different-history background.
5. [Derived public execution summary](public-execution-summary.json) and the actual source manifest. Raw cloud logs, connection details, deployment/shutdown receipts and operational wrappers remain local. The summary explicitly identifies that limitation and retains the original combined job exit code 1. Both training endpoints completed before the documented inference assertion; [local completion](training-and-own-inference-complete.json), [repair](prediction-interface-repair.json) and [budget amendment](inference-budget-amendment.json) preserve subsequent completion and actual counts. `reference_read: false` describes a capture instant, not a statement that later evaluation never happened.
6. [Conditional numerical decision](numerical-sensitivity-decision.json). It is not a new qualification gate or automatic interface-retraining authorization.

The complete inclusion list is [selected-artifacts.json](selected-artifacts.json). Raw endpoint payloads, archived runtime publication flags and historical P_F decisions are retained rather than rewritten. The [reproduction guide](../reproducibility.md) separates a cheap saved-evidence redraw from a separately authorized scientific repeat.
