interface:
  display_name: "ResearchStudio Paper Search"
  short_description: "Explicit-only, project-gated academic paper search"
  default_prompt: "Use $paper-search only after active_phase.md explicitly authorizes literature work."

policy:
  allow_implicit_invocation: false
