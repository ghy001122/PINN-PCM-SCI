# 项目执行规则

本文件是 [完整研究规范](docs/governance/PINN_PCM_Norms_2026-08-14.md) 的执行索引，不是第二套规范。规范原件由用户提供，来源为 `E:\PINN × Phase_Change_Materials\PINN_PCM_Norms_2026-08-14.md`；仓库副本于 2026-08-16 纳入项目。若摘要与完整规范存在差异，以完整规范为准；硬约束冲突必须交由用户决定。

## 1. 任务准入

- 开始前读取 `active_phase.md`、`PROJECT_STATE.md` 和 `docs/plans/NEXT_ACTIONS.md`。
- 当前阶段之外的行为不得静默启动。
- 重大科学任务先提交简洁、可执行的具体计划；影响科学核心、物理拓扑、参数唯一性、动态方程或预算的选择需用户确认。

## 2. 论文与方法边界

- 每项研究工作必须映射到与论文相关的主张/方程/主图/表/消融/泛化/局限性/审稿问题/复现材料。
- 传统求解器不能替代 PINN 的核心贡献。
- 复杂架构本身不是创新。完整方法须对强直接基线证明预声明增量，并至少包含一个隔离关键机制的消融。
- 主体物理采用二维及以上器件结构，闭合各物理场因果链；新增物理模块必须有必要性、接口和可检验增量。

## 3. 数据与评价

- 小 case 先检查方程、单位、边界、守恒、数量级、接口和数值稳定性，再进入昂贵运行。
- 按完整物理 case 或真实实体划分训练、验证和测试；测试信息不得参与训练、阈值选择或超参数选择。
- formal OOD、最强直接基线、关键消融和完整组合在正式扩展前预声明。
- 反问题先做可辨识性分析，不能用网络复杂度掩盖不可辨识。

## 4. 证据与表述

- 分开记录：实现状态、执行状态、数值有效性、证据身份和科学 claim 状态。
- 合成数值、原始文献事实、项目研究事实不得互相冒充。
- 强结论必须证据充分。
- 负面结果保留适用边界并按停止规则收口；不得制造正向结果或暗中重开已停止路线。

## 5. 来源与学术道德

- 优先使用原始论文、官方补充材料、公开源数据、作者代码和权威数据库。
- 直接迁移模块与修改后的适配模块分开记录，保存来源、许可、接口、改动和预期能力。
- 禁止编造数据、曲线、引文、许可、参数、实验验证或优先权；内部材料和 AI 内容只能作为线索，不能单独支撑外部科学事实。

## 6. 执行与止损

- 使用有预算、有检查点、有停止条件的闭环；禁止无休止的超参数搜索、救援 seed、无授权重复运行或开放式长任务。
- 只处理阻塞当前论文目标的问题，不因邻近问题扩张任务。
- 不以字节、token、行数、文件数、装饰性评分、重复审计或反复 SHA/哈希验证来判断研究是否成功。
- 达到预声明的通过、失败或阻塞条件即收口，并给出最小下一步决策。

## 7. 科研与工程分离

- 科学成果先在本地形成并核验；提交、推送、PR、合并和 CI 是独立工程操作。
- Git 或 CI 成功只能证明交付状态，不能证明科学主张。
- 治理改动与科学代码、配置、数据、证据和实验保持清楚边界；不为治理仪式额外增加科研工作。
- 权威文档发生变化时运行 `.\.venv\Scripts\python.exe -m pinn_pcm_sci.document_consistency --root .`，确保阶段、状态、唯一 live plan、ADR、链接和实验账本一致。

## 8. 冲突处理

发现明显冲突时，报告：冲突规则、拟执行动作、受影响的论文/证据目标、可选的最小调整。未得到用户明确决定前，不执行与硬约束冲突的部分。
